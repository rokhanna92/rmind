<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- RM.bg from lib/ui/design.dart. The app is dark only, so the launch
         window must be too, otherwise every cold start flashes white first. -->
    <color name="rmind_bg">#111318</color>

    <!-- RM.seed. The launcher icon's background layer, so the app tile matches
         the accent the whole interface is built from. -->
    <color name="ic_launcher_background">#4C6EF5</color>
</resources>
